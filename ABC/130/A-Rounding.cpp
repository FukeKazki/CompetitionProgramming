#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <list>
#include <cmath>
#include <iomanip>

using namespace std;

int main() {
    int x, a;
    cin >> x >> a;
    if(x >= a) {
        cout << 10 << endl;
    } else {
        cout << 0 << endl;
    }
    return 0;
}